//create a function for a button that, when clicked, plays sound (specifically for mobile browser restrictions)
function mobileCheerPlay(){
	//initialize a variable to the cheering audio
	var cheer_audio = document.getElementById('crowd_cheering')

	//play the audio
	cheer_audio.play();
}